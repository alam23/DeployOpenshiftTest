apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maven-pvc
  labels:
    sandbox: learn-pipelines
    learn-pipelines: workspace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi